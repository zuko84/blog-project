<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
  <a class="navbar-brand" href="/"><i class="fas fa-code"></i> BlogApp</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/blogs">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-dark" href="/blogs/news">Create Blog</a>
      </li>
      
    <% if(currentUser) { %>
      <li class="nav-item">
        <a class="nav-link text-dark" href="https://github.com/zuko84/blog-project">Git Source</a>
      </li>
      
      <li class="nav-item">
        <a class="nav-link disabled" href="#">About</a>
      </li>
      <% }; %>
    </ul>
    
    <ul class="navbar-nav ml-auto">
    <% if(!currentUser) { %>
      <li class="nav-item">
        <a class="nav-link text-dark" href="/login">LogIn</a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-dark" href="/register">Register</a>
      </li>
      <% } else { %>
      <li class="nav-item">
        <a class="nav-link text-dark" href="#">Sign In as <strong><%= currentUser.username %></strong></a>
      </li>
      
      <li class="nav-item">
        <a class="nav-link text-dark" href="/logout">LogOut</a>
      </li>
      <% }; %>
    </ul>
    
  </div>
</nav>

<div class="container margin-top w-50">
 <% if(error && error.length > 0) { %>
  <div class="alert alert-info" role="alert">
   <p class="text-center display-5 text-dark"><%= error %></p>
   <p class="text-center display-5 text-dark"><%= error.message %></p>
  </div>
 <% } %>
 
 <% if(success && success.length > 0) { %>
  <div class="alert alert-dark text-dark" role="alert">
   <p class="text-center display-5 text-dark"><%= success %></p>
   <p class="text-center display-5 text-dark"><%= success.message %></p>
  </div>
 <% } %>
 
<% if(warn && warn.length > 0) { %>
  <div class="alert alert-secondary" role="alert">
   <p class="text-center display-5 text-dark"><%= warn %></p>
  </div>
 <% } %>
 


 
</div>